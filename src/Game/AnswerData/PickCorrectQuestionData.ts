//The pickCorrectQuestionData for both pick correct and pick correct long question types

import { answer, quizData } from "../quiz/quizDataBase";

export default interface pickCorrectQuestionData extends quizData<answer[]> {}